# Backend API base URL
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Google OAuth Web client ID (same value as the backend's GOOGLE_CLIENT_ID).
# Create at: Google Cloud Console → APIs & Services → Credentials → OAuth client ID
# → Web application. Add http://localhost:3000 to Authorized JavaScript origins.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
